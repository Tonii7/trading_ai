import sys
from pathlib import Path

# ────────────────────────────────────────────
# 1. Fix Python path so "trading_ai" is visible
# ────────────────────────────────────────────
# Current file:
# src/trading_ai/services/gmail/run_gmail_listener.py
#
# We need to reach root: trading_ai/
# which is 3 levels up
BASE_DIR = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(BASE_DIR))

print("🔧 PYTHONPATH FIXED. Base dir:", BASE_DIR)

# ────────────────────────────────────────────
# 2. Now import the listener
# ────────────────────────────────────────────
from trading_ai.services.gmail.gmail_listener import listen_gmail

# ────────────────────────────────────────────
# 3. Run
# ────────────────────────────────────────────
if __name__ == "__main__":
    print("📨 Starting Gmail listener...")
    listen_gmail()
